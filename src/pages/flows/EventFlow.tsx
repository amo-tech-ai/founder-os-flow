import PlaceholderPage from "../Placeholder";

const EventFlow = () => (
  <PlaceholderPage
    title="Event Flow"
    description="Plan and execute memorable startup events. From demo days to launch parties, AI helps manage every detail."
    category="AI Flows"
  />
);

export default EventFlow;
